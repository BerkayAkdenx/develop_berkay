import 'package:flutter/material.dart';

class ProfilePageDividerWidget extends StatelessWidget {
  const ProfilePageDividerWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Colors.grey,
      height: 31,
    );
  }
}
